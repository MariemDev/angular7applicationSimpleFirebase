// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyAfe_y7rFHlofk78r-pXlc6hydw3It5X9I",
    authDomain: "bird-galaxy-52391.firebaseapp.com",
    databaseURL: "https://bird-galaxy-52391.firebaseio.com",
    projectId: "bird-galaxy-52391",
    storageBucket: "bird-galaxy-52391.appspot.com",
    messagingSenderId: "105651064003",
    appId: "1:105651064003:web:d983dace384f6a01"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
